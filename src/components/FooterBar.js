import React from 'react';
import '../css/App.css';

const FooterBar = (props) => {

  /**
   * @description Create the footer bar containing the link to the About page
   * @returns {HTMLDivElement} Main application page
   * @memberof FooterBar
   */
  return (
    <div>
      <h4>About</h4>
    </div>
  )
}

export default FooterBar;
