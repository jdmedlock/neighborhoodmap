import React from 'react';
// import PropTypes from 'prop-types';
// import { Route } from 'react-router-dom';
import '../css/App.css';

const PlacePage = (props) => {

  /**
   * @description Create the place page which provides the user with detailed
   * information about the selected place on the map
   * @returns {HTMLDivElement} Main application page
   * @memberof PlacePage
   */
  return (
    <div>
      <h1>Place fields and info goes here</h1>
    </div>
  )
}

export default PlacePage;
